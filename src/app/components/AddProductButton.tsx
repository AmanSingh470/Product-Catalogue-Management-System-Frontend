import React from 'react'

export default function AddProductButton() {
    return (
        <button
            className="bg-red-600 hover:bg-red-700 rounded-3xl p-2 text-white mb-3 font-bold cursor-pointer"
            id="large-add-product-btn">
            Add Product
        </button>
    )
}
